Django>=6.1,<6.2
django-filter>=26.1
django-simple-history>=3.13
Pillow>=10.0
pandas>=2.0
openpyxl>=3.1

# Storage backends — only the one you actually use needs real credentials.
# Both install safely even if unused (see settings.py for how the switch works).
django-cloudinary-storage>=0.3
cloudinary>=1.40
django-storages[s3]>=1.14
reportlab>=4.4
gunicorn>=23.0
whitenoise>=6.7
dj-database-url>=2.3
psycopg2-binary>=2.9
